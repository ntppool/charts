apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geoip.fullname" . }}-test-connection"
  labels:
{{ include "geoip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "geoip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
